version: '3.8'

services:
  website:
    build: .
    container_name: synabit-website
    ports:
      - "80:3000"
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - NODE_ENV=production
